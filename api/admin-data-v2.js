import { createClient } from '@supabase/supabase-js';
const env=n=>{if(!process.env[n])throw Error(`Missing ${n}`);return process.env[n]};
const allowed=new Set(['profiles','direct_conversations','direct_messages','reports','support_tickets','support_messages','admin_audit_logs','site_settings','products','coupons']);
export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Método não permitido.'});try{const h=req.headers.authorization||'';if(!h.startsWith('Bearer '))return res.status(401).json({error:'Não autenticado.'});const url=env('SUPABASE_URL'),anon=createClient(url,env('SUPABASE_ANON_KEY'),{auth:{persistSession:false}}),{data:{user},error:ae}=await anon.auth.getUser(h.slice(7));if(ae||!user)return res.status(401).json({error:'Sessão inválida.'});const db=createClient(url,env('SUPABASE_SERVICE_ROLE_KEY'),{auth:{persistSession:false}}),{data:ok,error:pe}=await db.rpc('is_admin',{uid:user.id});if(pe)return res.status(500).json({error:'Não foi possível verificar a permissão administrativa.'});if(!ok)return res.status(403).json({error:'Acesso negado.'});const resource=String(req.query.resource||'');if(!allowed.has(resource))return res.status(400).json({error:'Recurso administrativo inválido.'});let q=db.from(resource).select('*').limit(Math.min(Math.max(Number(req.query.limit)||500,1),1000));for(const k of ['id','user_id','ticket_id','conversation_id'])if(req.query[k])q=q.eq(k,String(req.query[k]));if(req.query.ids){const a=String(req.query.ids).split(',').filter(Boolean).slice(0,1000);if(a.length)q=q.in('id',a)}if(req.query.user_ids){const a=String(req.query.user_ids).split(',').filter(Boolean).slice(0,1000);if(a.length)q=q.in('user_id',a)}if(req.query.order)q=q.order(String(req.query.order),{ascending:String(req.query.asc||'false')==='true'});const {data,error}=await q;if(error)return res.status(500).json({error:error.message});return res.status(200).json({data:Array.isArray(data)?data:[]})}catch(e){console.error('admin-data-v2',e);return res.status(500).json({error:e?.message||'Erro interno.'})}}
