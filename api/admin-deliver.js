import { createClient } from '@supabase/supabase-js';
const env=n=>{if(!process.env[n])throw Error(`Missing ${n}`);return process.env[n]};
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Método não permitido.'});try{const h=req.headers.authorization||'';if(!h.startsWith('Bearer '))return res.status(401).json({error:'Não autenticado.'});const url=env('SUPABASE_URL'),anon=createClient(url,env('SUPABASE_ANON_KEY'),{auth:{persistSession:false}}),{data:{user},error:ae}=await anon.auth.getUser(h.slice(7));if(ae||!user)return res.status(401).json({error:'Sessão inválida.'});const db=createClient(url,env('SUPABASE_SERVICE_ROLE_KEY'),{auth:{persistSession:false}}),{data:allowed,error:pe}=await db.rpc('is_admin',{uid:user.id});if(pe)return res.status(500).json({error:'Não foi possível verificar a permissão administrativa.'});if(!allowed)return res.status(403).json({error:'Acesso negado.'});const id=String(req.body?.orderId||'').trim(),note=String(req.body?.deliveryNote||'').trim();if(!id)return res.status(400).json({error:'Pedido inválido.'});const {data:order,error:oe}=await db.from('orders').select('id,status,user_id').eq('id',id).single();if(oe||!order)return res.status(404).json({error:'Pedido não encontrado.'});if(!['PAID','PROCESSING'].includes(order.status))return res.status(409).json({error:'Somente pedidos pagos ou em processamento podem ser entregues.'});const now=new Date().toISOString(),{error}=await db.from('orders').update({status:'DELIVERED',delivery_note:note||null,delivered_at:now,delivered_by:user.id,updated_at:now}).eq('id',id);if(error)return res.status(500).json({error:error.message||'Não foi possível confirmar a entrega.'});await db.from('admin_audit_logs').insert({admin_id:user.id,action:'ORDER_DELIVERED',target_user_id:order.user_id,metadata:{order_id:id,delivery_note:note||null},created_at:now});await db.from('notifications').insert({user_id:order.user_id,title:'Pedido enviado',message:'Seu pedido foi marcado como entregue.','type':'order',order_id:id});return res.status(200).json({ok:true,status:'DELIVERED'})}catch(e){console.error('admin-deliver',e);return res.status(500).json({error:e?.message||'Erro interno.'})}}
