import { createClient } from '@supabase/supabase-js';
const env=n=>{if(!process.env[n])throw Error(`Missing ${n}`);return process.env[n]};
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Método não permitido.'});try{const h=req.headers.authorization||'';if(!h.startsWith('Bearer '))return res.status(401).json({error:'Não autenticado.'});const anon=createClient(env('SUPABASE_URL'),env('SUPABASE_ANON_KEY'),{auth:{persistSession:false}});const {data:{user}}=await anon.auth.getUser(h.slice(7));if(!user)return res.status(401).json({error:'Sessão inválida.'});const db=createClient(env('SUPABASE_URL'),env('SUPABASE_SERVICE_ROLE_KEY'),{auth:{persistSession:false}});const {data:profile}=await db.from('profiles').select('is_admin').eq('id',user.id).single();if(!profile?.is_admin)return res.status(403).json({error:'Acesso negado.'});const id=String(req.body?.orderId||'').trim(),note=String(req.body?.deliveryNote||'').trim();if(!id)return res.status(400).json({error:'Pedido inválido.'});const {data:order,error:oe}=await db.from('orders').select('id,status').eq('id',id).single();if(oe||!order)return res.status(404).json({error:'Pedido não encontrado.'});if(order.status!=='PAID')return res.status(409).json({error:'Somente pedidos pagos podem ser confirmados como entregues.'});const {error}=await db.from('orders').update({status:'DELIVERED',delivery_note:note||null,delivered_at:new Date().toISOString(),delivered_by:user.id,updated_at:new Date().toISOString()}).eq('id',id);if(error)return res.status(500).json({error:'Não foi possível confirmar a entrega.'});return res.status(200).json({ok:true,status:'DELIVERED'})}catch(e){console.error('admin-deliver',e);return res.status(500).json({error:'Erro interno.'})}}
