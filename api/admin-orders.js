import { createClient } from '@supabase/supabase-js';
const env=n=>{if(!process.env[n])throw Error(`Missing ${n}`);return process.env[n]};
export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Método não permitido.'});try{const h=req.headers.authorization||'';if(!h.startsWith('Bearer '))return res.status(401).json({error:'Não autenticado.'});const url=env('SUPABASE_URL'),anon=createClient(url,env('SUPABASE_ANON_KEY'),{auth:{persistSession:false}}),{data:{user},error:authError}=await anon.auth.getUser(h.slice(7));if(authError||!user)return res.status(401).json({error:'Sessão inválida.'});const db=createClient(url,env('SUPABASE_SERVICE_ROLE_KEY'),{auth:{persistSession:false}}),{data:allowed,error:ae}=await db.rpc('is_admin',{uid:user.id});if(ae)return res.status(500).json({error:'Não foi possível verificar a permissão administrativa.'});if(!allowed)return res.status(403).json({error:'Acesso negado.'});const {data,error}=await db.from('orders').select('id,user_id,robux,method,amount,status,roblox_username,gamepass_url,delivery_note,created_at,updated_at,delivered_at,delivered_by').order('created_at',{ascending:false}).limit(500);if(error)return res.status(500).json({error:'Não foi possível carregar os pedidos.'});return res.status(200).json({orders:Array.isArray(data)?data:[]})}catch(e){console.error('admin-orders',e);return res.status(500).json({error:e?.message||'Erro interno.'})}}
