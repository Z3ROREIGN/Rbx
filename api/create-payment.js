import { createClient } from '@supabase/supabase-js';

const digits=v=>String(v||'').replace(/\D/g,'').slice(0,11);const json=(res,status,body)=>res.status(status).json(body);const dbError=e=>[e?.code,e?.message,e?.details,e?.hint].filter(Boolean).join(' — ').slice(0,500)||'erro de banco';
function validCPF(v){const d=digits(v);if(d.length!==11||/^(\d)\1+$/.test(d))return false;let s=0;for(let i=0;i<9;i++)s+=Number(d[i])*(10-i);let r=(s*10)%11;if(r===10)r=0;if(r!==Number(d[9]))return false;s=0;for(let i=0;i<10;i++)s+=Number(d[i])*(11-i);r=(s*10)%11;if(r===10)r=0;return r===Number(d[10])}
function env(name){const value=process.env[name];if(!value)throw new Error(`Missing ${name}`);return value}
export default async function handler(req,res){if(req.method!=='POST')return json(res,405,{error:'Método não permitido.'});let stage='request';try{const token=String(req.headers.authorization||'').replace(/^Bearer\s+/i,'');if(!token)return json(res,401,{error:'Faça login para continuar.'});const url=env('SUPABASE_URL'),anon=env('SUPABASE_ANON_KEY');const authClient=createClient(url,anon,{auth:{persistSession:false}});stage='authentication';const {data:authData,error:authError}=await authClient.auth.getUser(token);if(authError||!authData.user)return json(res,401,{error:'Sessão inválida. Entre novamente.'});const client=createClient(url,anon,{auth:{persistSession:false},global:{headers:{Authorization:`Bearer ${token}`}}});stage='validation';const {robux,method,robloxUsername,payerName,payerDocument,gamepassUrl,confirmRobloxName}=req.body||{};const qty=Number(robux),username=String(robloxUsername||'').trim(),name=String(payerName||'').trim(),cpf=digits(payerDocument);if(!Number.isInteger(qty)||qty<1000||qty>50000||qty%1000!==0)return json(res,400,{error:'Quantidade inválida.'});if(!['Gamepass','Robux Plus'].includes(method))return json(res,400,{error:'Tipo de produto inválido.'});if(!/^[A-Za-z0-9_]{3,20}$/.test(username))return json(res,400,{error:'Usuário do Roblox inválido.'});if(name.split(/\s+/).length<2)return json(res,400,{error:'Informe o nome completo.'});if(!validCPF(cpf))return json(res,400,{error:'CPF inválido.'});if(method==='Robux Plus'&&String(confirmRobloxName||'').trim().toLowerCase()!==username.toLowerCase())return json(res,400,{error:'Confirme exatamente o nome do Roblox.'});stage='catalog';const {data:product,error:productError}=await client.from('products').select('name,method,robux,price,active').eq('method',method).eq('robux',qty).eq('active',true).maybeSingle();if(productError)return json(res,500,{error:`Erro ao consultar o catálogo: ${dbError(productError)}`});if(!product)return json(res,409,{error:'Este produto não está disponível no momento.'});stage='misticpay';const amount=Number(product.price),orderId=crypto.randomUUID(),ci=env('MISTICPAY_CLIENT_ID'),cs=env('MISTICPAY_CLIENT_SECRET'),webhook=process.env.MISTICPAY_WEBHOOK_URL||`${process.env.SITE_URL||'https://bestrobux.vercel.app'}/api/misticpay-webhook`;let mp;try{mp=await fetch('https://api.misticpay.com/api/transactions/create',{method:'POST',headers:{ci,cs,'Content-Type':'application/json'},body:JSON.stringify({amount,payerName:name.slice(0,100),payerDocument:cpf,transactionId:`BR-${orderId}`,description:`Best Robux - ${qty} Robux - ${method}`,projectWebhook:webhook}),signal:AbortSignal.timeout(15000)})}catch(e){return json(res,502,{error:`Não foi possível conectar ao serviço de pagamento: ${e?.message||'falha de conexão'}`})}const result=await mp.json().catch(()=>({}));if(!mp.ok)return json(res,502,{error:result?.message||result?.error||`MisticPay retornou HTTP ${mp.status}.`});const d=result?.data||result;if(!d?.transactionId||(!d.copyPaste&&!d.copy_paste&&!d.qrCodeBase64&&!d.qr_code_base64&&!d.qrcodeUrl))return json(res,502,{error:'A cobrança foi criada sem dados PIX válidos.'});stage='database-order';const {error}=await client.from('orders').insert({id:orderId,user_id:authData.user.id,robux:qty,method,amount,roblox_username:username,payer_name:name,payer_document:cpf,status:'PENDING',payment_transaction_id:String(d.transactionId),pix_copy_paste:d.copyPaste||d.copy_paste||null,pix_qr_code:d.qrCodeBase64||d.qr_code_base64||d.qrcodeUrl||null,gamepass_url:method==='Gamepass'?(String(gamepassUrl||'').trim()||null):null});if(error)return json(res,500,{error:`Erro interno no banco (database-order): ${dbError(error)}`});await client.from('notifications').insert({user_id:authData.user.id,title:'Pedido pendente',message:`Seu pedido de ${qty.toLocaleString('pt-BR')} Robux foi criado e está aguardando pagamento.`,type:'order',order_id:orderId});return json(res,201,{orderId,transactionId:String(d.transactionId),amount,status:'PENDING',copyPaste:d.copyPaste||d.copy_paste||null,qrCode:d.qrCodeBase64||d.qr_code_base64||d.qrcodeUrl||null})}catch(error){console.error('create-payment:',stage,error);return json(res,500,{error:`Erro interno ao criar pagamento (${stage}).`})}}
